/**
 * Camada de domínio (pura, sem I/O nem React). Na P0 há apenas a versão do app;
 * as regras portadas do nativo (RunnerEngine, métricas, export) chegam na P1+.
 */
export const APP_VERSION = "0.1.0";
